#include <stdio.h>
#include "queue.h"
#include "stack.h"
#include "pibonacci.h"


int main() {
	// --------------------- pibonacci side ---------------- 

	/*
	_pibonacci_t * pibo;
	pibo = pibonacci_initialization();

	insert_pibonacci(pibo);
	sum_pibonacci(pibo);
	print_pibonacci(pibo);
	print_sum_pibonacci(pibo);
	*/

}
